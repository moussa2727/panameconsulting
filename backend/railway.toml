[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile"
buildPath = "."  # ← Ajoutez cette ligne

[deploy]
numReplicas = 1

[env]
PORT = "10000"
NODE_ENV = "production"

[variables]
NODE_ENV = "production"
PORT = "10000"